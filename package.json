{
	"name": "hover-highlight",
	"version": "1.0.0",
	"description": "Obsidian plugin that adds a soft cursor glow effect to Canvas",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && cp src/styles.css styles.css",
		"lint": "tsc --noEmit -skipLibCheck"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.17.19",
		"obsidian": "^1.4.11",
		"tslib": "^2.6.0",
		"typescript": "^5.1.6"
	}
}
